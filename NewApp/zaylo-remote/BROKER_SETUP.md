# MQTT Broker Setup

This copy is kept inside the website workspace, so it must not contain live
broker endpoints, usernames, passwords, or production infrastructure notes.

The public demo uses `demo-broker.local` and mocked preview state. Real broker
hostnames and credentials should be injected only from a private deployment
environment or local developer configuration that is not committed into the
website tree.
